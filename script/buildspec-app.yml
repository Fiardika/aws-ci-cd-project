version: 0.2
phases:
  pre_build:
    commands:
    - aws ecr get-login-password --region ${AWS_DEFAULT_REGION} | docker login --username AWS --password-stdin ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com
    - REPOSITORY_URI=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/${IMAGE_REPO_NAME}
    - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c 1-7)
  build:
    commands:
    - docker build -t $REPOSITORY_URI:$IMAGE_TAG .
    - docker tag $REPOSITORY_URI:$IMAGE_TAG $REPOSITORY_URI:latest
  post_build:
    commands:
    - docker push $REPOSITORY_URI:$IMAGE_TAG
    - docker push $REPOSITORY_URI:latest
    - echo "Deploying to EC2..."
    - chmod +x script/deploy.sh
    - ls -la ~/.ssh/
    - cat ~/.ssh/id_rsa
    - ssh -v -o StrictHostKeyChecking=no ubuntu@${EC2_HOST} 'echo "SSH connection successful"'
    - ssh -o StrictHostKeyChecking=no ubuntu@${EC2_HOST} 'bash -s' < script/deploy.sh
  install:
    commands:
    - aws secretsmanager get-secret-value --secret-id EC2_SSH_KEY --query SecretString --output text > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
